<Window x:Class="StickyNoteV2.Views.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sticky Note"
        Width="250" Height="250"
        MinWidth="150" MinHeight="100"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        Background="Transparent"
        Opacity="0"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Button Style for Title Bar -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3"
                                Padding="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#30000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Color Menu Item Style -->
        <Style x:Key="ColorMenuItem" TargetType="MenuItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                Width="24" Height="24"
                                Margin="2"
                                CornerRadius="4"
                                BorderThickness="2"
                                BorderBrush="Transparent"
                                Cursor="Hand">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Border with rounded corners -->
    <Border x:Name="MainBorder" 
            Background="#FFF59D" 
            CornerRadius="4"
            BorderThickness="1"
            BorderBrush="#40000000">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              BlurRadius="10" 
                              ShadowDepth="2" 
                              Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" 
                  x:Name="TitleBar" 
                  Background="Transparent"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Drag Area -->
                <Border Grid.Column="0" Background="Transparent"/>

                <!-- New Note Button -->
                <Button Grid.Column="1" 
                        Style="{StaticResource TitleBarButton}"
                        Click="NewNote_Click"
                        ToolTip="New Note">
                    <TextBlock Text="+" FontWeight="Bold" FontSize="16"/>
                </Button>

                <!-- Pin Button -->
                <Button Grid.Column="2" 
                        x:Name="PinButton"
                        Style="{StaticResource TitleBarButton}"
                        Click="Pin_Click"
                        ToolTip="Pin on top">
                    <TextBlock x:Name="PinIcon" Text="📌" FontSize="12"/>
                </Button>

                <!-- Close Button -->
                <Button Grid.Column="3" 
                        Style="{StaticResource TitleBarButton}"
                        Click="Close_Click"
                        ToolTip="Delete Note">
                    <TextBlock Text="✕" FontWeight="Bold"/>
                </Button>
            </Grid>

            <!-- Note Content -->
            <TextBox Grid.Row="1"
                     x:Name="NoteTextBox"
                     Background="Transparent"
                     BorderThickness="0"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="12,4,12,12"
                     FontFamily="Segoe UI"
                     FontSize="14"
                     TextChanged="NoteTextBox_TextChanged"/>

            <!-- Context Menu -->
            <Grid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Colors">
                        <MenuItem.Template>
                            <ControlTemplate TargetType="MenuItem">
                                <Border Padding="8,4">
                                    <StackPanel>
                                        <TextBlock Text="Colors" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <WrapPanel>
                                            <MenuItem Style="{StaticResource ColorMenuItem}" 
                                                      Background="#FFF59D" 
                                                      Click="Color_Click" Tag="#FFF59D"/>
                                            <MenuItem Style="{StaticResource ColorMenuItem}" 
                                                      Background="#F48FB1" 
                                                      Click="Color_Click" Tag="#F48FB1"/>
                                            <MenuItem Style="{StaticResource ColorMenuItem}" 
                                                      Background="#90CAF9" 
                                                      Click="Color_Click" Tag="#90CAF9"/>
                                            <MenuItem Style="{StaticResource ColorMenuItem}" 
                                                      Background="#A5D6A7" 
                                                      Click="Color_Click" Tag="#A5D6A7"/>
                                            <MenuItem Style="{StaticResource ColorMenuItem}" 
                                                      Background="#FFCC80" 
                                                      Click="Color_Click" Tag="#FFCC80"/>
                                            <MenuItem Style="{StaticResource ColorMenuItem}" 
                                                      Background="#CE93D8" 
                                                      Click="Color_Click" Tag="#CE93D8"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </MenuItem.Template>
                    </MenuItem>
                </ContextMenu>
            </Grid.ContextMenu>
        </Grid>
    </Border>
</Window>
