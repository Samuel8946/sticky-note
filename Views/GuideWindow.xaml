<Window x:Class="StickyNoteV2.Views.GuideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to StickNote"
        Width="520" Height="680"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="22,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1F2937"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FFFDF8" CornerRadius="20" BorderBrush="#E7E0D4" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="32" ShadowDepth="8" Opacity="0.28"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="28,22,28,16"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFF4C2" Offset="0"/>
                        <GradientStop Color="#FFD6E7" Offset="0.5"/>
                        <GradientStop Color="#D6EBFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="STICKNOTE" FontSize="11" FontWeight="Bold"
                                   Foreground="#6B7280"/>
                        <TextBlock x:Name="TitleText" Text="Welcome" FontSize="26"
                                   FontWeight="SemiBold" Foreground="#111827" Margin="0,6,0,0"/>
                        <TextBlock x:Name="SubtitleText" Text="A 30-second tour of the essentials."
                                   FontSize="13.5" Foreground="#4B5563" TextWrapping="Wrap" Margin="0,6,40,0"/>
                    </StackPanel>
                    <TextBlock Text="✕" FontSize="16" Foreground="#6B7280" HorizontalAlignment="Right"
                               VerticalAlignment="Top" Cursor="Hand" Padding="8"
                               MouseLeftButtonDown="Close_Click"/>
                </Grid>
            </Border>

            <!-- Example card -->
            <Border Grid.Row="1" Margin="28,8,28,8" Background="#F8F5EE" CornerRadius="16" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="12" ClipToBounds="True">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="2" Opacity="0.12"/>
                        </Border.Effect>
                        <Image x:Name="ExampleImage" Stretch="Uniform" Margin="12"/>
                    </Border>

                    <TextBlock Grid.Row="1" x:Name="BodyText"
                               Text="Click a note and start typing. Everything saves automatically."
                               TextWrapping="Wrap" FontSize="13.5" Foreground="#374151"
                               Margin="4,14,4,0" TextAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Dots -->
            <StackPanel Grid.Row="2" x:Name="DotsPanel" Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,4,0,8"/>

            <!-- Footer -->
            <Border Grid.Row="3" Padding="24,10,24,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox x:Name="ShowAgainCheck" Content="Show this at startup"
                              VerticalAlignment="Center" IsChecked="True"
                              Foreground="#6B7280" FontSize="12"/>

                    <Button Grid.Column="1" x:Name="BackButton" Content="Back"
                            Style="{StaticResource GhostButton}" Margin="0,0,8,0"
                            Click="Back_Click"/>
                    <Button Grid.Column="2" x:Name="NextButton" Content="Next"
                            Style="{StaticResource PrimaryButton}" Click="Next_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
